# ⚠️ Deprecated

This directory is no longer in use. The canonical console apps are:

- **`apps/console`** — Main console application (Vercel deployment)
- **`apps/console-proxy`** — Backend API server (Docker/self-hosted deployment)

This directory will be removed in a future cleanup.
