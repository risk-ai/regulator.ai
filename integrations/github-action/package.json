{
  "name": "vienna-os-govern-action",
  "version": "1.0.0",
  "description": "GitHub Action for governed CI/CD with Vienna OS",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/index.ts -o dist",
    "test": "vitest"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
