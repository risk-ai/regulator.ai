# ============================================================================
# Vienna Console Frontend Production Configuration Template
# ============================================================================
# Copy to .env.production and fill in actual values before deployment
# Note: Frontend env vars are PUBLIC (embedded in built JavaScript)
# Never include sensitive data here

# ============================================================================
# API Configuration
# ============================================================================
# Use relative paths for same-origin deployment (recommended)
VITE_API_BASE=/api/v1

# Public URL (used for absolute links if needed)
VITE_PUBLIC_URL=https://app.<DOMAIN>

# ============================================================================
# Build Configuration
# ============================================================================
# No additional build-time configuration required
# Vite automatically handles production optimizations
